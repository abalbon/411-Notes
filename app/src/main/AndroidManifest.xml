<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.csuf.cpsc41101.testapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PersonDetailActivity"
            android:parentActivityName=".SummaryLVActivity"
            android:launchMode="singleTop">
        <!--This will not call on create, instead it will just do onStart and onResume
                Any data changes will not be displayed-->
        </activity>
        <activity android:name=".DynamicContent0926Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>